/*package edu.uchicago.cs.java.finalproject.game.model;

/**
 * Created by Joceyn on 11/23/14.
 */
/*public class Explosion extends movableAdapter {

   private int Point pntCenter;
   private int nExpiry;

   public Explosion(Bullet bullet) {
       pntCenter= bullet.getCenter();
       nExpiry = 20
   }

   public void expire() {
       super.expire();
       nExpiry--;
       nRadius -=10
       if (nExpiry<1) {
           nRadius-=10;
       }
   }

  public void draw(Graphics g) {
      super.draw(g);
      g.setColor(new Coloer(Game.R.nextInt(256),new Coloer(Game.R.nextInt(256),new Color(Game.R.nextInt(256));
  }


}
**/